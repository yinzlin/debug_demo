fn main() {
    let message = "VSCode Rust Debug Test";
    println!("{}", message);  // 在这一行左侧点击添加断点
    println!("{}", message);  // 在这一行左侧点击添加断点
    println!("{}", message);  // 在这一行左侧点击添加断点
    let sum = 1 + 2;
    println!("1 + 2 = {}", sum);
}